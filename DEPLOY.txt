Pulse Receipt — Arc mainnet deploy (chainId 5042)
Expected funded deployer (do not send keys): 0x60C35aBAF3B7eA46646e8e796181c9b583C191C7
Gas: native USDC, 18 decimals. ERC-20 interface 6 decimals at 0x3600000000000000000000000000000000000000.

# from this directory, with PRIVATE_KEY already in the environment:
forge create src/PulseReceipt.sol:PulseReceipt \
  --rpc-url https://rpc.mainnet.arc.io \
  --private-key "$PRIVATE_KEY" \
  --broadcast \
  --with-gas-price 30000000000

# verify
forge verify-contract <DEPLOYED> src/PulseReceipt.sol:PulseReceipt \
  --chain-id 5042 \
  --verifier blockscout \
  --verifier-url https://explorer.arc.io/api/ \
  --watch
